{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^2.2.1"
  },
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=20.x"
  },
  "name": "sv-previous"
}
