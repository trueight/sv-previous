{
  "private": true,
  "license": "MIT",
  "version": "0.1.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^2.2.1"
  },
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "name": "sv-previous",
  "description": "SV Previous Mono Repo"
}
